import { split, toUpper } from "no-case";
export function constantCase(input, options) {
    const upper = toUpper(options?.locale);
    return split(input, options).map(upper).join("_");
}
//# sourceMappingURL=index.js.map